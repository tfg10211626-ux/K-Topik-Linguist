# Copy to backend/.env（或專案根目錄 .env）並填入金鑰；勿留空行占住變數。
# 亦支援環境變數 GOOGLE_API_KEY（與 Google AI Studio 金鑰相同）。
#
# Gemini 使用 gRPC：未設定時 app.py 會預設 GRPC_ENABLE_FORK_SUPPORT=0（減少與 fork/reloader 衝突）。
# GRPC_ENABLE_FORK_SUPPORT=0
#
# 本機 python app.py：可設 FLASK_DEBUG=1；已關閉 reloader，改碼後請手動重啟。
# FLASK_DEBUG=1
#
# 公開／ngrok 試用（於 backend 目錄、已 pip install -r requirements.txt）：PORT 可自訂
# gunicorn -w 2 --threads 4 -t 120 -b 0.0.0.0:${PORT:-5000} app:app
GEMINI_API_KEY=
# 預設為 gemini-3.1-flash-lite（亦見 ktl_backend.config.DEFAULT_GEMINI_MODEL）
GEMINI_MODEL=
# 選填：額外允許的「前端」origin（逗號分隔）。後端已預設允許 http://127.0.0.1:5500，勿把 API 網址填在這裡。
# KTL_CORS_ORIGINS=http://127.0.0.1:5500
# FLASK_SECRET_KEY=
#
# Google 登入（OAuth 網頁用戶端；授權重新導向須含 /api/auth/google/callback）
# GOOGLE_CLIENT_ID=
# GOOGLE_CLIENT_SECRET=
# 選填：固定 callback（ngrok／正式網域建議設定）
# GOOGLE_OAUTH_REDIRECT_URI=http://127.0.0.1:5000/api/auth/google/callback
# KTL_AUTH_SUCCESS_REDIRECT=/index.html
# Azure Speech（TTS）；未設 SPEECH_URL 時需 SPEECH_REGION。選填自訂 endpoint（私有雲等）。
# SPEECH_KEY=
# SPEECH_REGION=eastasia
# SPEECH_URL=
# ElevenLabs（情境造句「聽我示範」TTS）
# ELEVENLABS_API_KEY=
#
# Supabase（請用 Project Settings → API 的 anon public，eyJ 開頭；勿用 sb_publishable_ / service_role）
# SUPABASE_URL=https://xxxx.supabase.co
# SUPABASE_ANON_KEY=eyJhbGciOi...
# 或 SUPABASE_KEY=eyJhbGciOi...